# Caddy — proxy reverso com HTTPS interno automático para a rede local.
# Aceita qualquer host/IP (sem precisar de DNS ou nome específico).
:443 {
    reverse_proxy app:8000
    tls internal
}

:80 {
    reverse_proxy app:8000
}
