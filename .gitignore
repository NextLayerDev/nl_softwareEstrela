# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Env / segredos
.env
.env.prod

# Dados (planilhas reais do cliente — não versionar)
data/*.xlsx
data/*.xls

# Binário standalone do Tailwind (não versionar). O output.css É versionado
# porque a imagem Docker não compila o CSS no build.
/tailwindcss

# Artefatos de importação
relatorio_inconsistencias*.xlsx

# Dumps de imagens / arquivos grandes soltos (não versionar)
*.zip
app/imagens produtos.zip

# SQL de recuperação de imagens (bytes embutidos — não versionar)
data/recuperacao/

# SO
.DS_Store
